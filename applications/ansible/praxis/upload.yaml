# Files to upload to the server during deployment
# Uses git archive to respect .gitignore
uploads:
  - source: "."       # git archive path (relative to repo root)
    prefix: "platformer"  # directory name inside the archive (extracts to /opt/platformer)
    dest: "/opt/platformer"
