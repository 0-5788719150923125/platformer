collections:
  - name: amazon.aws
    version: ">=8.0.0"
  - name: ansible.windows
    version: ">=2.5.0"
  # storage-mount playbook: filesystem creation + idempotent mount/fstab management
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
