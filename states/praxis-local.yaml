services:
  compute:
    praxis-local:
      type: localhost
      applications:
        - type: shell
          script: "./launch compose --juliet --no-dashboard --headless --infer-every 10 --data-path '../platformer' --reset"
          params:
            WORKING_DIR: /home/crow/repos/praxis
            STOP_CMD: "./launch stop"
